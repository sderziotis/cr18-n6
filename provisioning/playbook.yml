# ─────────────────────────────────────────────
#  Module 6 / N6a - Traffic Analysis CTF1
#  Topology: cr18-N6-v1  (pentester + targetvm)
#
#  Required file next to this playbook:
#    files/orion-internal.pcapng
# ─────────────────────────────────────────────

- name: init machine pentester
  hosts:
    - pentester
  become: true

  roles:
    - role: sandbox-logging
      slf_destination_port: '{% if hostvars["man"] is defined -%} 514 {%- else -%} 515 {%- endif %}'
    - role: user-access
      user_access_username: user
      user_access_password: 'password123'
      user_access_sudo: true

  tasks:

    # ─── apt housekeeping ────────────────────────────────────────
    - name: Disable unattended-upgrades during provisioning
      ansible.builtin.systemd:
        name: unattended-upgrades
        state: stopped
      failed_when: false

    - name: Kill leftover apt/dpkg processes
      ansible.builtin.shell: |
        pkill -9 apt || true
        pkill -9 dpkg || true
      changed_when: false
      failed_when: false

    - name: Wait for apt lock to be released
      ansible.builtin.shell: >
        while fuser /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock
        /var/cache/apt/archives/lock /var/lib/dpkg/lock >/dev/null 2>&1; do
          sleep 3
        done
      changed_when: false
      failed_when: false

    # ─── analysis tooling ────────────────────────────────────────
    # Preseed before installing: wireshark-common otherwise opens an
    # interactive debconf dialog and provisioning hangs. This also
    # creates the "wireshark" group used below.
    - name: Preseed wireshark-common (no interactive dialog)
      ansible.builtin.debconf:
        name: wireshark-common
        question: wireshark-common/install-setuid
        value: 'true'
        vtype: boolean

    - name: Install analysis tools
      ansible.builtin.apt:
        name:
          - wireshark
          - tshark
          - unzip
          - file
          - xxd
        state: present
        update_cache: 'yes'

    - name: Add trainee to the wireshark group
      ansible.builtin.user:
        name: user
        groups: wireshark
        append: true

    # ─── case files ──────────────────────────────────────────────
    # Root-owned and read-only: a trainee who overwrites the capture
    # cannot lock themselves out of the final level.
    - name: Create case directory
      ansible.builtin.file:
        path: /home/user/case
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Deploy the Orion Retail capture
      ansible.builtin.copy:
        src: files/orion-internal.pcapng
        dest: /home/user/case/orion-internal.pcapng
        owner: root
        group: root
        mode: '0444'

    - name: Verify the capture deployed intact
      ansible.builtin.stat:
        path: /home/user/case/orion-internal.pcapng
        checksum_algorithm: sha256
      register: pcap_stat
      failed_when: >
        pcap_stat.stat.checksum !=
        'a750921b6b95aa35f2a97d7646bb8d1ff29eaa4d186429ea9eca40810dba78ab'

    - name: Deploy the case brief
      ansible.builtin.copy:
        dest: /home/user/case/README.txt
        owner: user
        group: user
        mode: '0644'
        content: |
          ORION RETAIL LTD - INCIDENT REVIEW
          ==================================

          The internal web admin panel was accessed without authorization.
          A capture was running on the internal interface of the server
          srv-web01 (172.20.30.10) at the time.

          Evidence:  ~/case/orion-internal.pcapng   (read-only)
          Tools:     Wireshark, tshark, unzip

          The LAN in the capture, 172.20.30.0/24, no longer exists. This is
          an offline review - there is nothing to connect to.

          Answers in the training portal are case-sensitive.

    - name: Add a case-folder shortcut to the trainee shell
      ansible.builtin.blockinfile:
        path: /home/user/.bashrc
        marker: "# {mark} KYPO N6A CASE SHORTCUT"
        block: |
          alias case='cd /home/user/case'
        create: false

    # ─── desktop ─────────────────────────────────────────────────
    - name: Create Polkit rule to disable colord password prompts
      ansible.builtin.copy:
        dest: /etc/polkit-1/rules.d/45-allow-colord.rules
        mode: '0644'
        content: |
          polkit.addRule(function(action, subject) {
              if ((action.id == "org.freedesktop.color-manager.create-device" ||
                   action.id == "org.freedesktop.color-manager.create-profile" ||
                   action.id == "org.freedesktop.color-manager.delete-device" ||
                   action.id == "org.freedesktop.color-manager.delete-profile" ||
                   action.id == "org.freedesktop.color-manager.modify-device" ||
                   action.id == "org.freedesktop.color-manager.modify-profile") &&
                  subject.isInGroup("user")) {
                  return polkit.Result.YES;
              }
          });

    - name: Ensure xfconf config directory exists
      ansible.builtin.file:
        path: /home/user/.config/xfce4/xfconf/xfce-perchannel-xml
        state: directory
        owner: user
        group: user
        mode: '0755'
        recurse: true

    - name: Set XFCE DPI to 170
      ansible.builtin.copy:
        dest: /home/user/.config/xfce4/xfconf/xfce-perchannel-xml/xsettings.xml
        owner: user
        group: user
        mode: '0644'
        content: |
          <?xml version="1.0" encoding="UTF-8"?>

          <channel name="xsettings" version="1.0">
            <property name="Xft" type="empty">
              <property name="DPI" type="int" value="170"/>
            </property>
          </channel>

    - name: Enable OpenSSH server
      ansible.builtin.systemd:
        name: ssh
        enabled: true
        state: started


- name: init machine targetvm
  hosts:
    - targetvm
  become: true

  roles:
    - role: user-access
      user_access_username: user
      user_access_password: 'password123'
      user_access_sudo: true

  tasks:
    - name: Disable unattended-upgrades during provisioning
      ansible.builtin.systemd:
        name: unattended-upgrades
        state: stopped
      failed_when: false

    # N6a is offline forensics - targetvm only routes network-3 and
    # needs nothing beyond the base image. Add N6b tasks below.

    - name: Enable OpenSSH server
      ansible.builtin.systemd:
        name: ssh
        enabled: true
        state: started


- name: Disable unattended apt upgrades
  hosts: pentester,targetvm,man
  become: true
  tasks:
    - name: Disable unattended upgrades in apt periodic config
      copy:
        dest: /etc/apt/apt.conf.d/20auto-upgrades
        owner: root
        group: root
        mode: "0644"
        content: |
          APT::Periodic::Update-Package-Lists "0";
          APT::Periodic::Unattended-Upgrade "0";

    - name: Disable apt daily timers
      systemd:
        name: "{{ item }}"
        state: stopped
        enabled: false
        masked: true
      loop:
        - apt-daily.timer
        - apt-daily-upgrade.timer
